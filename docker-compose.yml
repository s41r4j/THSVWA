version: '3.8'

services:
  thsvwa:
    build: .
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
    restart: unless-stopped
    container_name: thsvwa-app
    networks:
      - thsvwa-network

networks:
  thsvwa-network:
    driver: bridge
